# Help texts for ConfigKeys
help_dbgrazer.auth.ldap.linkName = Connection name to use for LDAP user authentication
help_dbgrazer.auth.ldap.userBaseDN = LDAP base DN for looking up user accounts, relative to the baseDN
help_dbgrazer.auth.ldap.userFilter = LDAP filter for looking up user accounts
help_dbgrazer.auth.ldap.userPattern = Pattern for converting local user names to LDAP user names

# de.tweerlei.dbgrazer.core.model.ConnectionType
LDAP = LDAP

# de.tweerlei.dbgrazer.core.query.ResultType
LDAP_LOOKUP = LDAP entry
LDAP_LIST = LDAP child entries
LDAP_SEARCH = LDAP search (subtree)
LDAP_ONELEVEL = LDAP search (1 level)
LDAP_OBJECT = LDAP search (base)
LDAP_ATTR = LDAP attribute values

# Help texts for de.tweerlei.dbgrazer.core.query.ResultType
help_LDAP_LOOKUP = Queries attributes of a single LDAP entry (SELECT attributNames FROM baseDN)
help_LDAP_LIST = Queries attributes of immediate children of an LDAP entry (SELECT attributNames FROM baseDN)
help_LDAP_SEARCH = Queries attributes of LDAP entries matching a search (SELECT attributNames FROM baseDN WHERE ldapFilter)
help_LDAP_ONELEVEL = Queries attributes of LDAP entries matching a search (SELECT attributNames FROM baseDN WHERE ldapFilter)
help_LDAP_OBJECT = Queries attributes of LDAP entries matching a search (SELECT attributNames FROM baseDN WHERE ldapFilter)
help_LDAP_ATTR = Lists the values of a single attribute (SELECT attributName FROM baseDN)

# UI texts
ldapBrowser = LDAP browser
ldapRoot = Root
entriesTab = Entries
filesTab = Entry
