# Help texts for ConfigKeys
help_dbgrazer.auth.ldap.linkName = Name der Verbindung, die für LDAP-Benutzerauthentifizierung verwendet werden soll
help_dbgrazer.auth.ldap.userBaseDN = LDAP DN, unter dem Benutzereinträge gesucht werden, relativ zu baseDN
help_dbgrazer.auth.ldap.userFilter = LDAP-Filter für Benutzereinträge
help_dbgrazer.auth.ldap.userPattern = Muster, um lokale Benutzernamen in LDAP-Benuzernamen zu konvertieren

# de.tweerlei.dbgrazer.core.model.ConnectionType
LDAP = LDAP

# de.tweerlei.dbgrazer.core.query.ResultType
LDAP_LOOKUP = LDAP-Eintrag
LDAP_LIST = LDAP-Untereinträge
LDAP_SEARCH = LDAP-Suche (tief)
LDAP_ONELEVEL = LDAP-Suche (1 Ebene)
LDAP_OBJECT = LDAP-Suche (Basis)
LDAP_ATTR = LDAP-Attributwerte

# Hilfetexte für de.tweerlei.dbgrazer.core.query.ResultType
help_LDAP_LOOKUP = Liefert die Attribute eines einzelnen Eintrags (SELECT attributNamen FROM baseDN)
help_LDAP_LIST = Liefert die Attribute direkter Untereinträge eines Eintrags (SELECT attributNamen FROM baseDN)
help_LDAP_SEARCH = Liefert die Attribute passender Einträge zu einer Suche (SELECT attributNamen FROM baseDN WHERE ldapFilter)
help_LDAP_ONELEVEL = Liefert die Attribute passender Einträge zu einer Suche (SELECT attributNamen FROM baseDN WHERE ldapFilter)
help_LDAP_OBJECT = Liefert die Attribute passender Einträge zu einer Suche (SELECT attributNamen FROM baseDN WHERE ldapFilter)
help_LDAP_ATTR = Liefert die Werte eines einzelnen Attributs (SELECT attributName FROM baseDN)
